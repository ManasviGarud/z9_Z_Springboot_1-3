spring.application.name=z9_Z_Springboot_1-3
# AWS Configuration (Injected via environment variables or system properties)
aws.region=${AWS_REGION}
aws.s3.bucket=${AWS_S3_BUCKET}
aws.sns.topicArn=${AWS_SNS_TOPIC_ARN}
aws.dynamodb.tableName=${AWS_DYNAMODB_TABLE_NAME}

# AWS Credentials (Do NOT hardcode — should come from environment variables)
aws.accessKeyId=${AWS_ACCESS_KEY_ID}
aws.secretAccessKey=${AWS_SECRET_ACCESS_KEY}

